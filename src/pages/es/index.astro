---
import HomePage from '../../components/HomePage.astro';
import { getPage, getSiteSettings } from '../../lib/content';

const [page, settings] = await Promise.all([getPage('es', 'home'), getSiteSettings('es')]);
if (page.pageType !== 'home') throw new Error('Invalid Spanish home content');
---

<HomePage locale="es" {page} {settings} />
